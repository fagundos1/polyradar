{
  "buildCommand": "pnpm install --frozen-lockfile && pnpm run build && mkdir -p .vercel/output/static && cp -r dist/* .vercel/output/static/ && mkdir -p .vercel/output/functions/api && cp -r api/*.js .vercel/output/functions/api/ 2>/dev/null || true",
  "framework": null
}

